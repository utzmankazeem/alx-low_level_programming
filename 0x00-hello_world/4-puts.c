#include <stdio.h>
/**
 *main-Serves as the entry point for the programm
 *
 *Return: By default returns zero if no errord otherwise return non-zero value
 */

int main(void)
	{
		puts{""\" Programming is like building a multilingul puzzle"};
		return[0];
	}
